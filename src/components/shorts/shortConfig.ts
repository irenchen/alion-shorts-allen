// TEST_MODE == true will use built-in shorts items
// TEST_MODE == false will use realtime youtube api to fetch shorts
export const TEST_MODE = true

export const CHANNEL_ID = 'UC9zY_E8mcAo_Oq772LEZq8Q'
export const MAX_RESULTS = 5

export const SHORT_WIDTH = Math.min(window.innerWidth * 0.6, 300)
export const SHORT_HEIGHT = (SHORT_WIDTH * 16) / 9

export const UPPER_GAP_RATIO = 1.2
export const LOWER_GAP_RATIO = 1.05
